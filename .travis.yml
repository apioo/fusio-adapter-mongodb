language: php
php:
  - 7.0
  - 7.1
services:
  - mongodb
before_install:
  - phpenv config-rm xdebug.ini
  - phpenv config-add tests/config.ini
install:
  - composer install
script:
  - vendor/bin/phpunit
